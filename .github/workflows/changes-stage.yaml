name: Change Notification (Stage)
"on":
  push:
    branches:
      - "main"
    paths:
      - 'apps/*/ap-stage/version.yaml'
env:
  PATTERN: 'apps/*/ap-stage/version.yaml'
  ENV_NAME: 'Stage'
jobs:
  announce-change:
    uses: ./.github/workflows/change-notify.yaml
    with:
      pattern: "${{ env.PATTERN }}"
      env_name: "${{ env.ENV_NAME }}"
